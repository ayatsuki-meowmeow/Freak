{
  "name": "@repo/api",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "lint": "eslint --max-warnings 0",
    "check-types": "tsc --noEmit",
    "validate": "bun run lint && bun run check-types",
    "sample-seed": "bun run scripts/seed.sample.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "@repo/schema": "workspace:*",
    "dotenv": "^17.4.1",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.12",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "eslint": "^9.39.1",
    "tsx": "^4.21.0"
  }
}
